Xtreme10.0 - Painter's Dilemma

Binary Quilts

// Xtreme 10.0 - Full Adder (implement general full adder)

// Xtreme9.0 - Mr. Pippo's Pizza (Katalan numbers)

// Xtreme9.0 - Digit Fun! (Number values can be stored as Strings)

// Xtreme9.0 - Alice and Bob Play Sheldon's Favorite Game
(A simple graph that the out-degree of all the nodes is bigger than one must contain a cycle).

// Xtreme 10.0 - Collecting Gold (Dijkstra)